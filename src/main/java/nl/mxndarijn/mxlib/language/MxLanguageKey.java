package nl.mxndarijn.mxlib.language;

/** Contract for language keys that map to entries in the language configuration. */
public interface MxLanguageKey {
    String key(); // e.g., "no-permission"
}